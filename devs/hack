// Make buildings roofs transparent
Object.keys(a("03f4982a")).forEach(function(key) {

  if( a("03f4982a")[key].ceiling ) {

    if( a("03f4982a")[key].ceiling.imgs ) {
      Object.keys(a("03f4982a")[key].ceiling.imgs).forEach( function(item) {
        a("03f4982a")[key].ceiling.imgs[item].alpha = 0.5;
      });
    }
  }
});
